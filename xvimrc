" No status bar
set laststatus=0

" Search
set incsearch
set ignorecase
set smartcase
set wrap
set wrapscan
set gdefault

" Setup the number line
set number
set relativenumber

" Use the system pasteboard
set clipboard=unnamed

" Comments
nmap gcc :xccmd commentAndUncommentCurrentLines<CR>
vmap gc  :xccmd commentAndUncommentCurrentLines<CR>

" Open quickly
nmap <space>f :xcmenucmd Open Quickly...<CR>

" Issues
nmap <space>n :nissue<CR>
nmap <space>p :pissue<CR>

" Toggle breakpoint at current line
nmap ,b :xccmd toggleBreakpointAtCurrentLine<CR>

" Rename refactor
nmap ,r :xccmd refactorRename<CR>

" Better navigation
nmap H ^
nmap L $

" Window navigation
nmap <C-h> <C-w>h
nmap <C-l> <C-w>l

" Make computers make sense
nmap D d$
nmap Y y$

" I'd like the following command to save too
" it doesn't seem like xvim supports this: \| update<CR>
vmap s :sort ui<CR>

" vim-unimpared knock off
nmap ]e "udd"up
nmap [e "uddk"uP

" Jump to counterpart
nmap <C-j> :xccmd jumpToPreviousCounterpart<CR>
nmap <C-k> :xccmd jumpToNextCounterpart<CR>

" Show documentation
nmap <space>d :xcmenucmd Quick Help for Selected Item<CR>
