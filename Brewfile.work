# Shells & Prompt
brew "starship"
brew "zsh-autosuggestions"
brew "zsh-syntax-highlighting"

# Git
brew "git-delta"
brew "gh"

# CLI Tools
brew "bat"
brew "eza"
brew "fzf"
brew "ripgrep"
brew "zoxide"

# Swift / Xcode
brew "xcbeautify"

# VS Code extensions must be installed manually via the VS Code UI.
# `code --install-extension` fails on corporate networks due to ZScaler SSL inspection.
